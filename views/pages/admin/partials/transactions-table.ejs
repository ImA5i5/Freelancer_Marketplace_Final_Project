<div class="overflow-x-auto bg-white rounded-xl shadow">
  <table class="min-w-full text-sm">
    <thead class="bg-gray-50 text-gray-700">
      <tr>
        <th class="p-3 text-left">Type</th>
        <th class="p-3 text-left">Client</th>
        <th class="p-3 text-left">Freelancer</th>
        <th class="p-3 text-left">Milestone</th>
        <th class="p-3 text-center">Amount</th>
        <th class="p-3 text-center">Status</th>
        <th class="p-3 text-center">Created</th>
      </tr>
    </thead>

    <tbody>
      <% transactions.forEach(t => { %>
        <tr class="border-t hover:bg-gray-50">
          <td class="p-3"><%= t.type %></td>
          <td class="p-3"><%= t.client?.fullName || '-' %></td>
          <td class="p-3"><%= t.freelancer?.fullName || '-' %></td>
          <td class="p-3"><%= t.milestone?.title || '-' %></td>

          <td class="p-3 text-center">₹<%= t.amount %></td>

          <td class="p-3 text-center">
            <span class="px-2 py-1 rounded-full text-xs font-semibold
              <%= t.status === 'completed'
                    ? 'bg-green-100 text-green-700'
                    : t.status === 'pending'
                    ? 'bg-yellow-100 text-yellow-700'
                    : 'bg-red-100 text-red-700'
              %>">
              <%= t.status %>
            </span>
          </td>

          <td class="p-3 text-center"><%= new Date(t.createdAt).toLocaleString() %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

<!-- ========================= -->
<!-- 🔵 Pagination Buttons -->
<!-- ========================= -->
<div class="flex justify-center mt-6 gap-2">

  <% for (let i = 1; i <= pagination.totalPages; i++) { %>
    <button
      class="pagination-btn px-3 py-1 border rounded
        <%= i === pagination.page
              ? 'bg-blue-600 text-white border-blue-600'
              : 'bg-gray-100 hover:bg-gray-200 border-gray-300' %>"
      data-page="<%= i %>">
      <%= i %>
    </button>
  <% } %>

</div>
